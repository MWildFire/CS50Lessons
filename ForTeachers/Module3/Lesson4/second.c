#include <stdio.h>
#include <cs50.h>

int fact(int n)
{
    int t, answer;
    answer == 1;
    for (t = 1; t <= n; t++)
    {
        answer = answer *t;
    }
    return answer;
}

int factr(int n)
{
    int answer;
    if (n == 1)
        return (1)
    answer = factr(n-1) * n;
        return (answer);
}

int main()
{

}